apiVersion: v1
kind: Service
metadata:
  name: agentflow
  namespace: aiops
  labels:
    app: agentflow
spec:
  selector:
    app: agentflow
  ports:
  - name: http
    port: 8000
    targetPort: 8000
