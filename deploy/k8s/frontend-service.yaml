apiVersion: v1
kind: Service
metadata:
  name: agentflow-frontend
  namespace: aiops
  labels:
    app: agentflow-frontend
spec:
  selector:
    app: agentflow-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: NodePort
